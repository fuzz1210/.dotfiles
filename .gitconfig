# .gitconfig



[user]
	name = fuzz1210
	email = fuzz1210@gmail.com
[color]
	ui = true
[alias]
	cf = config
	cfg = config --global
	lg = log --graph --date=iso --pretty=format:'%C(white reverse) %h %Creset --------------------------------------------------------------------------------%n%C(green)%an%Creset %C(cyan)%ad (%ar)%Creset%C(yellow)%d%Creset%n%n%C(white)%s%Creset%n'
	lgn = log --graph --date=iso --name-status --pretty=format:'%C(white reverse) %h %Creset --------------------------------------------------------------------------------%n%C(green)%an%Creset %C(cyan)%ad (%ar)%Creset%C(yellow)%d%Creset%n%n%C(white)%s%Creset%n'
	br = branch
	bra = branch -a
	ch = checkout
	st = status
	df = diff -w -U0
	dfw = diff -w --color-words -U0
	ad = add
	rss = reset --soft
	rsm = reset --mixed
	rsh = reset --hard
	cm = commit -m
	cma = commit -a -m
	ps = push
	fc = fetch
	mg = merge
	pl = pull
[push]
	default = simple
[pager]
    log = diff-highlight | less
    show = diff-highlight | less
    diff = diff-highlight | less
